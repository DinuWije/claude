Match host "local-*,workspace-*,*.workspace.infra.dog"
    # Suppress SSH channel open-failed noise — messages originate from the
    # client-side RemoteForward in 00-common.config racing on reconnect.
    LogLevel ERROR
